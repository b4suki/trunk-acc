<%=  popup_title "Input Tipe kas"  %>

<div id="RB_body" style="width: 350px;">
  <% form_for(:accounting_cash, @accounting_cash, :url => {:action => 'create'})  do |f| %>
    <%= f.error_messages %>
    <%= render :partial => 'form', :locals => { :f => f } %>
    <p>
      <%= f.submit "Simpan" %>
    </p>
  <% end %>
</div>